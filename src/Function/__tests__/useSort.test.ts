import { expect, vi, it, describe, beforeEach, afterEach } from 'vitest';
import { useSort } from '../useSort';
const {setFilter} =useSort();

describe('useSort', () => {
it("フィルターの切り替え",()=>{
  setFilter("未")
  expect()
})

});
